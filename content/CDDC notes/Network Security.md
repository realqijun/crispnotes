### Sniffing
1. Attacker turns on promiscuous mode (accepts packets even if they are not for you)
	1. In a hub environment, packets are already broadcasted to every network
	2. In a switch environment, active attack to ARP protocol is needed

