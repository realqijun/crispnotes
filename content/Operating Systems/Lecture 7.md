## Contiguous Memory Scheme

## Buddy Scheme
 - Array list storing info about memory
 - Memory can be split into 2 iteratively, to get blocks to store info

## Lecture 8

## Disjoint Memory Scheme

## Page Table
 - Lookup mechanism
 - Logical memory(pages) are stored in physical memory(frames), and are retrieved from page table
 - Page logical, frame physical
 ![[Screenshot 2025-03-12 at 10.56.33.png]]
  - Page size is same as frame
  - Physical address = F x size of physical frame + Offset